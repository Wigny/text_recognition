import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:text_recognition/app/modules/leitor_placa/leitor_placa_page.dart';

main() {
  testWidgets('LeitorPlacaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LeitorPlacaPage(title: 'LeitorPlaca')));
    //  final titleFinder = find.text('LeitorPlaca');
    //  expect(titleFinder, findsOneWidget);
  });
}
